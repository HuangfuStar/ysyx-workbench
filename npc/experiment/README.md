# Digital Electric Experiments

Each subdirectory is an independent NVBoard experiment.

- `priority_encoder`: 8-to-3 priority encoder with LED and seven-segment output
- `vga`: static image output on the NVBoard VGA display
